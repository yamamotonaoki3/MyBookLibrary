# 近隣図書館の在庫確認 機能定義書

← [要件定義書に戻る](../requirements.md)

## 概要

ユーザーが近くの図書館を最大5館登録し、本の貸出状況をカーリル API でリアルタイム確認できる機能。

## 対象ユーザー

一般ユーザー・管理者（管理者は管理パネルから登録）

## 機能仕様

- ユーザーが近くの図書館を最大 5 館まで登録できる
  - 一般ユーザー：設定画面（`/settings`）から登録
  - 管理者：管理者パネル（`/admin`）から登録（管理者には設定画面がないため）
- 本一覧・本を探す・著者別書籍一覧・賞別作品一覧の各画面で「図書館の在庫を確認」ボタンを表示する
- カーリル API を使い、登録した図書館の貸出状況をリアルタイムで確認できる

| 状態 | 表示 |
| --- | --- |
| 貸出可 | 緑バッジ |
| 貸出中 | 赤バッジ |
| 蔵書なし | グレーバッジ |

- 貸出状況のバッジから図書館の予約ページへ直接遷移できる（外部サイト）
- ISBN のない書籍は在庫確認不可（バッジ非表示）
- カーリル API はポーリング方式のため、結果取得まで最大数秒かかる場合がある

## 関連画面

| パス | 画面名 |
| --- | --- |
| `/settings` | 設定画面（図書館の登録・削除） |
| `/books` | マイ本一覧（在庫確認ボタン） |
| `/books/search` | 本の検索（在庫確認ボタン） |
| `/favorite-authors/[authorId]` | 著者別書籍一覧（在庫確認ボタン） |
| `/awards` | 受賞作品一覧（在庫確認ボタン） |

## 関連 API エンドポイント

| メソッド | パス | 説明 |
| --- | --- | --- |
| GET | `/api/user-libraries` | 登録図書館一覧取得 |
| POST | `/api/user-libraries` | 図書館を登録（最大 5 件） |
| DELETE | `/api/user-libraries` | 図書館の登録解除 |
| GET | `/api/calil/libraries` | カーリル API で図書館を検索（地域・座標から候補を取得） |
| GET | `/api/calil/check` | カーリル API で図書館在庫確認（`isbn` / `libraries` クエリ） |

## 関連ドキュメント

- [API 仕様書](../api-spec.md) — カーリル API パラメータ・レスポンス形式
- [ER 図](../er-diagram.md) — Library テーブル構成
