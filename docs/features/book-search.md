# 本の一覧・検索 機能定義書

← [要件定義書に戻る](../requirements.md)

## 概要

楽天ブックス API を使った書籍検索・バーコードスキャン登録・マイ本一覧の絞り込み表示機能。

## 対象ユーザー

一般ユーザー

## 機能仕様

- マイ本一覧画面でステータス別・著者別に絞り込み表示できる
- 楽天ブックス API を使って書籍を検索し、読書ステータスに追加できる
- 検索結果は単行本優先（単行本 > 新書 > 文庫 > その他）・最古出版年でタイトル重複を除外する
- ISBN で直接書籍の詳細ページへ遷移できる（`/books/isbn/[isbn]`）
- カメラでバーコードをスキャンして ISBN を読み取り、書籍を検索できる
  - フォールバック：楽天で見つからない場合は国立国会図書館（NDL）API を使用
  - NDL 経由の場合は書影（表紙画像）なし
- 読書ステータス追加時、同タイトル・同著者の本が既に DB に登録済みの場合は版違い（単行本/文庫など）でも既存レコードに紐付ける（重複登録防止）
- 楽天ブックス API / NDL に見つからない書籍は、検索画面から手動でタイトル・著者・ISBN などを入力して登録できる（`POST /api/reading-status` で `source: "manual"` の書籍として作成される）。手動登録書籍の管理・マージは管理者が行う（[管理者機能 機能定義書](./admin.md) を参照）

### 書籍データ取得優先順位

| データ | 取得方法 |
| --- | --- |
| 書影・タイトル・著者名・出版年 | 楽天ブックス API（主） |
| ISBN 検索フォールバック | 国立国会図書館 SRU API |
| 著者検索フォールバック | 国立国会図書館 SRU API |

### 重複排除ロジック

1. タイトルを NFKC 正規化・スペース除去して比較
2. 同タイトルが複数ヒットした場合、単行本優先・最古出版年のものを採用

## 関連画面

| パス | 画面名 |
| --- | --- |
| `/books` | マイ本一覧 |
| `/books/search` | 本の検索（バーコードスキャン対応） |
| `/books/[id]` | 本の詳細 |
| `/books/isbn/[isbn]` | ISBN 書籍詳細 |

## 関連 API エンドポイント

| メソッド | パス | 説明 |
| --- | --- | --- |
| GET | `/api/books/search` | 楽天 API で書籍検索 |
| GET | `/api/books/isbn` | ISBN で書籍情報を取得 |
| GET | `/api/books/reading` | 読書中・読了の本一覧 |
| GET | `/api/books/[id]` | 本の詳細取得 |
| PATCH | `/api/books/[id]` | 本の編集（手動登録のみ） |
| DELETE | `/api/books/[id]` | 本の削除（手動登録のみ） |

## 関連ドキュメント

- [API 仕様書](../api-spec.md) — 検索クエリパラメータ・レスポンス形式
- [ER 図](../er-diagram.md) — Book テーブル構成
