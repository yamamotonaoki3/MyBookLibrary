# 読書ステータス管理 機能定義書

← [要件定義書に戻る](../requirements.md)

## 概要

本ごとに「未読・読みたい・読書中・読了」の4段階で読書状態を記録・変更できる機能。

## 対象ユーザー

一般ユーザー

## 機能仕様

- 本ごとに以下の4状態でステータスを記録できる

| ステータス値 | 表示名 |
| --- | --- |
| `unread` | 未読 |
| `want_to_read` | 読みたい |
| `reading` | 読書中 |
| `read` | 読了 |

- ステータスは何度でも変更できる（upsert）
- `unread` に戻すと `ReadingStatus` レコードが削除される（デフォルト状態 = レコードなし）
- ステータス登録時、同タイトル・同著者の本が既に DB に登録済みの場合は版違い（単行本/文庫など）でも既存レコードに紐付ける（重複登録防止）

## 関連画面

| パス | 画面名 |
| --- | --- |
| `/books` | マイ本一覧（ステータス別フィルター） |
| `/books/[id]` | 本の詳細（ステータスボタン） |
| `/awards` | 受賞作品一覧（各作品にステータスボタン） |
| `/favorite-authors/[authorId]` | 著者別書籍一覧（各書籍にステータスボタン） |
| `/books/search` | 本の検索（検索結果にステータスボタン） |

## 関連 API エンドポイント

| メソッド | パス | 説明 |
| --- | --- | --- |
| POST | `/api/reading-status` | 読書ステータス登録・更新（upsert） |

## 関連ドキュメント

- [API 仕様書](../api-spec.md) — リクエスト/レスポンス形式・バリデーション
- [ER 図](../er-diagram.md) — ReadingStatus テーブル構成
