# 認証・アカウント管理 機能定義書

← [要件定義書に戻る](../requirements.md)

## 概要

メールアドレス＋パスワード認証および Google OAuth によるログイン・ユーザー登録機能。不正ログイン対策・ロール管理・アカウント削除を含む。

## 対象ユーザー

一般ユーザー・管理者（ロールにより一部機能が異なる）

## 機能仕様

- メールアドレス＋パスワードでユーザー登録・ログインができる
- 新規登録画面で合言葉（秘密の言葉）を任意で設定できる（2〜50文字）。未設定のまま登録した場合は、設定画面から後で設定できる
- Google アカウントでログイン（OAuth）できる
- パスワードを忘れた場合、メールアドレス確認 → 合言葉（秘密の言葉）照合 → パスワード再設定の3ステップで、メール送信なしにパスワードを再設定できる
- ログイン中のユーザーは `/settings/change-password` から現在のパスワードを確認したうえで新しいパスワードに変更できる（一時パスワードと同一の値には変更できない）
- 管理者はユーザー管理画面から対象ユーザーのパスワードを一時パスワードで強制リセットできる。リセット後は `mustChangePassword` フラグが立ち、本人はパスワード変更を完了するまで通常操作ができない（本人には一時パスワードを知らせる通知が送られる）
- 設定画面（`/settings`）から合言葉を設定・変更できる（現在のパスワードでの本人確認が必要）。合言葉が未設定のユーザーがパスワードリセットを試みると、設定を促す通知が送られる
- 合言葉の照合を 10 回失敗するとロック（15 分間）される。ロックはパスワード側の `loginFailCount`／`lockedUntil` とは別に、合言葉専用の `secretWordFailCount`／`secretWordLockedUntil` で管理する
- ログイン失敗を 10 回繰り返すとアカウントが 15 分間ロックされる
- ロック中はログイン試行残回数を画面に表示する（`/api/auth/remaining-attempts`）
- ロール管理：一般ユーザー（`user`）と管理者（`admin`）を区別する
- 自分のアカウントを削除できる（一般ユーザーのみ。管理者ロールは削除不可）
- 削除時は関連する全データ（レビュー・いいね・通報・通知・読書ステータス・お気に入り著者・フォロー・セッション・アカウント）をトランザクションで一括削除する
- 設定画面（`/settings`）でアカウント情報（名前・メールアドレス・ロール）をモーダルで確認できる
- 管理者操作・合言葉更新などの重要イベントは監査ログ（`AuditLog`）に記録される。詳細は [管理者機能 機能定義書](./admin.md) を参照

### 認証フロー

**Credentials 認証**
1. メールアドレス＋パスワードで認証
2. ログイン失敗時：`loginFailCount` をインクリメント
3. 10 回失敗で `lockedUntil` を現在時刻 + 15 分に設定
4. ロック中はログイン不可（残り試行回数を `/api/auth/remaining-attempts` で取得可能）
5. パスワードリセット成功時に `loginFailCount` と `lockedUntil` をリセット

**パスワードリセット（合言葉方式）**

1. `step: "check"` — メールアドレスを確認。合言葉が未設定の場合は `SECRET_WORD_NOT_SET` エラーを返し、本人に設定を促す通知を送る
2. `step: "verifySecretWord"` — 合言葉を照合する。10 回失敗で 15 分ロック（`SECRET_WORD_LOCKED`）
3. `step: "reset"` — 合言葉を再検証したうえでパスワードを更新する（stateless 運用のため各ステップで再検証する）

**Google OAuth**
1. Google アカウントでログイン → `Account` テーブルに OAuth 情報を保存
2. Google アカウントユーザーはパスワードリセット不可（`GOOGLE_ACCOUNT` エラーを返す）

**セッション**
- JWT ストラテジー（`strategy: "jwt"`）
- トークンにユーザー ID（String 型）とロールを含める
- `/admin` パスへのアクセスは `role === "admin"` チェック

### セキュリティ仕様

- パスワードのハッシュ化（bcryptjs、salt: 12）
- 10 回失敗で 15 分ロック

## 関連画面

| パス | 画面名 |
| --- | --- |
| `/login` | ログイン画面 |
| `/register` | ユーザー登録画面 |
| `/forgot-password` | パスワードリセット画面 |
| `/settings` | 設定画面（アカウント情報確認・合言葉設定・削除） |
| `/settings/change-password` | パスワード変更画面 |

## 関連 API エンドポイント

| メソッド | パス | 説明 |
| --- | --- | --- |
| POST | `/api/auth/register` | ユーザー登録（合言葉は任意） |
| POST | `/api/auth/reset-password` | パスワードリセット（step: check/verifySecretWord/reset） |
| GET | `/api/auth/remaining-attempts` | ログイン試行残回数確認 |
| POST | `/api/user/secret-word` | 合言葉の設定・変更（現在のパスワードでの本人確認が必要） |
| POST | `/api/user/change-password` | ログイン中のパスワード変更 |
| POST | `/api/admin/users/[id]/reset-password` | 管理者によるパスワード強制リセット |
| DELETE | `/api/user/delete` | 自分のアカウント削除（管理者は不可） |

## 関連ドキュメント

- [API 仕様書](../api-spec.md) — バリデーションルール・エラーコード詳細
- [ER 図](../er-diagram.md) — User・Account・Session テーブル構成
